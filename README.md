# sptd_lock
a lightweight, namespace-aware Linux kernel module. It provides a sysfs interface (/sys/devices/platform/sptd_lock/lock_mgr) to orchestrate advisory locking between independent userspace processes, threads, and isolated Docker containers.
